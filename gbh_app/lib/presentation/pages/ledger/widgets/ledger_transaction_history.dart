import 'package:flutter/material.dart';

class LedgerTransactionHistory extends StatelessWidget {
  const LedgerTransactionHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('거래내역'),
    );
  }
}
